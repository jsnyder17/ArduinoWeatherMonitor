package enums;

public enum ConfigKey {
    USERNAME,
    PASSWORD,
    LATITUDE,
    LONGITUDE,
    STATE,
    COUNTY,
    UNITS,
    API_KEY,
    DB_PATH,
    USE_DB,
    USE_ARDUINO,
    COM_PORT,
    INTERVAL,
    DATA_SOURCES
}
